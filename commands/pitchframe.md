---
description: Generate a launch video for this codebase (optional free-form directive)
argument-hint: "[directive, e.g. \"focus on collaboration, pitching enterprise buyers\"]"
allowed-tools: Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch, Skill
---

Run the full Pitchframe pipeline on this project.

Founder directive (may be empty): $ARGUMENTS

Invoke the `pitchframe` skill and follow it end to end. Treat the directive above
as founder guidance for Layer 1 positioning — it outranks your own reading of the
codebase where the two conflict, and you note the tension in the brief rather than
silently resolving it.

Do not ask the founder any questions during the run. Every decision the pipeline
needs is one you make yourself. The only thing shown to the founder mid-run is the
positioning brief, and it is shown for information, not approval.
